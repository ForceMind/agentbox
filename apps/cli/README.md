# AgentBox CLI

The Phase 2 CLI validates the package entry point and output shape. `status` and `doctor` explicitly report that they are not implemented; they perform no host inspection or mutation.
