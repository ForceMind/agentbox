# AgentBox Worker

This is a supervised-process skeleton only. It can print its version, perform an in-process health check, start an idle event loop, and shut down on `SIGINT`/`SIGTERM`. It has no database, Job, Helper, Runtime, subprocess, or system-operation behavior.
