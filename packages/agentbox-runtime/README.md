# agentbox-runtime

Runtime Adapter interfaces and their future Codex/Claude implementations belong here. Phase 2 provides only a deliberately tiny protocol-shaped placeholder. It never discovers, starts, stops, pairs, authenticates, or invokes a real Runtime.
