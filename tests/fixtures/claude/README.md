# Claude CLI fixtures

Synthetic, redacted public-help and terminal-state examples used for capability
and conservative interaction parsing. They contain no host output, credentials,
tokens, project source, or private Claude configuration.
