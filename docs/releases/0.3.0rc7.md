# AgentBox 0.3.0rc7 — R11 composed failure-injection candidate

Planned release display/tag: `v0.3.0-rc.7`. This source increment does not
create a tag, publish a GitHub Release, install a systemd unit/socket, activate
a host, install a Chromium extension, handle a credential, or inherit real-host
qualification from any earlier candidate.

## Included software

- Deterministic, test-only composed failure injection for WAW admission, active
  relay, restart, shutdown, browser lifecycle and exact Stop behavior.
- Closed checkpoints and void gates, integer-nanosecond clocks, controlled
  partial writes, and dynamic canary scans across Audit, Jobs, logs,
  diagnostics, SQLite sidecars, browser DOM/storage and task owners.
- One version source: Python uses `0.3.0rc7`; root, Web and browser-extension
  packages use `0.3.0-rc.7`; the MV3 inert manifest maps it to `0.3.0.7`.

## Delivery evidence

PR #81 candidate `04ef0ae2b94e127cacc496e7876bd41cf203f43d`
(`test(waw): add rc7 composed failure injection`) completed the 20/20
exact-head CI matrix. It merged normally as
`b0eaef2e4e54cf1aba86e7669733d0adc885c1fb`; exact read-back and all six
standard post-main workflows are successful.

## Verification boundary

This is test-only software evidence. It exercises deterministic fixtures and
failure contracts; it does not establish a signed CRX/trustd deployment, real
provider enrollment, vendor CLI login, PTY/isolation, systemd/reboot evidence,
credential handling, host activation or product support.

## Not included

- R12 host activation and qualification, including real key custody, Chromium
  policy, Native Messaging/trustd install, CLI/PTY behavior and reboot recovery.
- rc8 artifact, upgrade and rollback rehearsal.
- rc9 full route/state localization.
