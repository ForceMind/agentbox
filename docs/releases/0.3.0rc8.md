# AgentBox 0.3.0rc8 — R11 artifact and operations rehearsal candidate

Planned release display/tag: `v0.3.0-rc.8`. This source increment does not
create a tag, publish a GitHub Release, activate a host, install a systemd
unit/socket or Chromium extension, handle a credential, or inherit real-host
qualification from any earlier candidate.

## Planned P0 rehearsal scope

- Build native helpers from unpacked artifact source and import only from an
  artifact wheelhouse environment.
- Drive artifact API, Runtime, RFC6455 and Unix-socket paths with a synthetic
  key/peer/trust/PTY harness through attach, input, output, resize, detach and
  exact Stop.
- Upgrade from the exact rc7 release-record predecessor
  `87f5bce964eba231a6a7ade73eaedac7e54646ae`, then roll back to that exact
  predecessor while verifying non-secret database, Project, Runtime-home and
  epoch canaries, receipts and journal integrity.
- One version source: Python uses `0.3.0rc8`; root, Web and browser-extension
  packages use `0.3.0-rc.8`; the MV3 inert manifest maps it to `0.3.0.8`.

## Current status

The frozen P0 contract and the first P1 artifact-execution checkpoint are
complete. Commit `3f97bf0848f8aa5e1cd2cfe0baf1227a062629f0` passed exact-head
Linux CI for candidate artifact-native provenance, no-index artifact-only
imports on CPython 3.11/3.12/3.13, and the non-skipping 3.11 synthetic
API/Runtime/RFC6455/PTY path. It records an artifact-local venv proof and
rejects site customization or checkout AgentBox imports.

Exact rc7/rc8 dual-artifact upgrade/rollback, dynamic canary evidence, the
final rc8 review/merge/read-back and all publication actions remain incomplete.

## Verification boundary

The planned rehearsal uses synthetic external inputs and artifact-only
environments. It does not establish signed CRX/trustd deployment, real provider
enrollment, vendor CLI login, PTY/isolation, systemd/reboot evidence, credential
handling, host activation or product support.

## Historical predecessor

rc7 delivered deterministic, test-only composed failure injection. Its release
record merged as `87f5bce964eba231a6a7ade73eaedac7e54646ae`; this historical
fact does not make the rc8 artifact/operations work complete.
